/*

 const ExerciseSchema = {
  id: "string", // Unique identifier
  name: "string", // Exercise name, e.g., "Pull-up"
  level: "string", // "beginner" | "intermediate" | "advanced";
  measurement: "string", //  "reps" | "seconds"
  type: "string", // "strength", "flexibility", "mobility"
  description: "string", // Optional description or tips
  targetMuscleGroups: ["string"], // Target muscles, e.g., ["back", "biceps"]
  equipment: ["string"], // Optional, e.g., ["pull-up bar", "rings"], empty if bodyweight
  sets: "number", // Number of sets
  reps: "number", // Reps per set
  extraWeight: {
    // Optional for weighted calisthenics
    value: "number",
    unit: "kg|lb",
  },
  dateCreated: "ISODate", // When exercise was added
  lastPerformed: "ISODate", // Optional last performed date
};


const Workout = {
  id: string;
  date: string; // when workout was done
  name: string;
  level: number;
  estimatedTime: number;
  exercises: 'ExerciseSchema[]';
};

*/
